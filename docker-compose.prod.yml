version: '3'
services:
  adventure-notes-api:
    build:
      context: .
    ports:
      - 4000:80
  adventure-notes-db:
    image: mongo:latest
    volumes:
      - adventure-notes-db:/data/db
volumes:
  adventure-notes-db: {}
